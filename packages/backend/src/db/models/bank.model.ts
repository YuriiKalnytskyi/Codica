import { EnhancedBase } from './enhanced-base';import { Entity, OneToMany } from 'typeorm';import { ApiColumn } from 'src/module/common/decorators';import { Transaction } from './transaction.model';@Entity()export class Bank extends EnhancedBase {  @ApiColumn(    { example: 'monoBank', description: 'Represents the name of the bank' },    { unique: true }  )  name: string;  @ApiColumn(    { example: '10000', description: `Represents the bank's current balance` },    { default: 0 }  )  balance: number;  @OneToMany(() => Transaction, (transaction) => transaction.bank, {    nullable: true,    cascade: ['remove']  })  transactions: Transaction[];}